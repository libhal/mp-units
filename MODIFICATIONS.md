# libhal modifications

This is a fork of mp-units modified to suite the needs of libhal. This fork
will be used to upload a variants of mp-units to the libhal repo.

Changes are:

- Set `freestanding: True` as default in the `conanfile.py`.
- Replaced `.github/` with the standard libhal upload.
